/**
 * Local development seed placeholder.
 * Populates availability / sample data once the database package is fully wired.
 */
console.log(
  JSON.stringify({
    ok: true,
    script: "seed-local",
    message: "No-op seed: database migrations and seed data land in a later mission.",
  }),
);
