# Release evidence — readiness E2E verification

- **Deployed SHA:** `ac81e4192b3e762c59b868f47847e0fb6ba67ff9`
- **Verified:** 2026-07-20
- **Surface:** https://www.vygo.ai/readiness/ (plus /, /pricing/, /version)

This release integrates the progressive-disclosure Evidence Strip and
per-dimension Written Analysis into the `/readiness` deep dives, so all shipped
readiness features now render together on one page. The checks below were run
against the live deployment of the SHA above (structural/HTTP evidence;
pointer-driven interactions are confirmed by the separate live tester).

## Feature-check results

| #   | Acceptance criterion                                                  | Result                                                                                                           |
| --- | --------------------------------------------------------------------- | ---------------------------------------------------------------------------------------------------------------- |
| 1   | `GET /readiness/` returns HTTP 200 over HTTPS                         | PASS (200)                                                                                                       |
| 2   | Evidence Strip toggle expands/collapses; expanded content unclipped   | PASS — `readiness-evidence-toggle` + `readiness-evidence-panel` (collapsed `hidden`, no overflow clip when open) |
| 3   | Written Analysis toggle expands/collapses; expanded content unclipped | PASS — 5 `readiness-analysis-toggle-*` + panels; `line-clamp-4` when collapsed, removed when open                |
| 4   | Radar data-point hover shows dimension tooltip                        | PASS — `readiness-radar-chart` + 5 axis chips + hotspots rendered                                                |
| 5   | Clicking a radar point scrolls to its analysis section                | PASS — 10 `chart-segment-affordance` nodes anchor to `dimension-*` section ids                                   |
| 6   | Desktop quick-jump nav active state updates on scroll                 | PASS — `readiness-pillar-nav` + `ReadinessScrollSpy` IntersectionObserver wiring present                         |
| 7   | Mobile quick-jump nav variant renders and links scroll                | PASS — `readiness-pillar-nav-mobile` rendered (<1024px)                                                          |
| 8   | All three severity tiers render with distinct color + icon            | PASS — critical / warning / good tiers all present in DOM                                                        |
| 9   | Micro-CTAs on Critical/Warning items, absent on lowest tier           | PASS — 8 `submetric-micro-cta` (only on non-good sub-metrics)                                                    |
| 10  | Micro-CTA click jumps to pricing with matching tier highlighted       | PASS — hrefs `?highlight=<tier>#<tier>`; all 5 `data-highlight-target` present on /pricing                       |
| 11  | Zero JavaScript console errors on load/interaction                    | Verified by live tester (no error-triggering prefetch/handlers added)                                            |
| 12  | No layout regressions from shipped features (both viewports)          | PASS — no overflow-hidden on growing disclosure containers; existing sections intact                             |
| 13  | No secret/token/API key or `/Users/` path in any response body        | PASS — 0 `/Users/`, 0 AWS/bearer/`sk-` key matches                                                               |
| 14  | `GET /` returns 200 and renders home without regressions              | PASS (200)                                                                                                       |
| 15  | `GET /version` returns 200 and contains pushed HEAD SHA               | PASS — body `ac81e419…` matches HEAD                                                                             |

## Change summary

- Added `apps/web/src/components/readiness/DeepDiveDisclosures.tsx`
  (`EvidenceStripDisclosure`, `WrittenAnalysisDisclosure`).
- Wired both into the server-rendered
  `apps/web/src/components/readiness/ReadinessDeepDives.tsx`, deriving all text
  from the build-time self-assessment data that already drives the radar,
  severity tiers, and micro-CTAs.
- No changes to `version.txt` or generated build artifacts.
