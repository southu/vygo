# /vibe-coding v1 → v2 inventory

**Source of truth for v1:** live `GET https://www.vygo.ai/vibe-coding` fetched
2026-07-29, cross-checked against the repository content that renders it —
`apps/web/src/app/vibe-coding/page.tsx`, `apps/web/src/content/vibe-coding.ts`,
and `apps/web/src/content/vibe-coding-modules.ts`. Live HTML and repo content
matched exactly (same hero copy, same non-negotiables, same
`vibe-coding-modules` JSON payload embedded in the page) — there was no drift
to reconcile before rewriting.

This inventory covers the **hub article at `/vibe-coding`** itself (the page
scoped by this mission's acceptance criteria), not the separate
`/vibe-coding/ratchet-guide/*` documentation pack, which is a linked-to but
independently-versioned pack (own `CHANGELOG.md`, own zip download) and is out
of scope for this rewrite.

Every row below is a section of the live v1 page, in document order.

| #   | Section (`data-section`)                                  | v1 content                                                                                                                                                                                                                                                                                       | Disposition in v2                | Reason                                                                                                                                                                                                                                                                                          |
| --- | --------------------------------------------------------- | ------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------ | -------------------------------- | ----------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| 1   | `hero`                                                    | Eyebrow "Vibe coding", H1 "Vibe coding that only moves forward", intro paragraph, 3 CTAs (Start free → guide zip, Read the guide, Rebuild checklist)                                                                                                                                             | **Kept, copy lightly refreshed** | Still an accurate one-line pitch for the system; CTAs and their hrefs are covered by `apps/web/e2e/vibe-coding-cta.spec.ts` and must not move                                                                                                                                                   |
| 2   | `setup-first` (Step 1: "Get set up first")                | 3-step `StepList` walking through downloading the guide pack and pasting a setup prompt                                                                                                                                                                                                          | **Kept as-is**                   | Onboarding mechanics didn't change; still the correct first action                                                                                                                                                                                                                              |
| 3   | `GuideOffer` component (no heading, sits below Step 1)    | Free v1.2 guide pack download offer card                                                                                                                                                                                                                                                         | **Kept as-is**                   | Download offer is independent of the article's prose; not part of this rewrite's scope                                                                                                                                                                                                          |
| 4   | `definition` ("What vibe coding is — and what it is not") | Two-column is/is-not list                                                                                                                                                                                                                                                                        | **Kept, wording unchanged**      | Definition still holds; none of the v2 additions contradict it                                                                                                                                                                                                                                  |
| 5   | `loop` ("The loop")                                       | 6-step diagram: Goal → Multi-step missions → Build → Live deploy gate → Test → Streak of passes, with a FAIL-note caption                                                                                                                                                                        | **Kept as-is**                   | The mission-level loop shape is unchanged; what changed is what happens _inside_ the Build and deploy-gate steps, which is exactly what the new "What changed since v1" section adds context for                                                                                                |
| 6   | `non-negotiables`                                         | 4 cards: Live is truth, `/version` must report the deploy SHA, no secrets in the builder environment, multi-step goals not mega-prompts                                                                                                                                                          | **Kept as-is**                   | All four are still true and still enforced; nothing in the ratchet-system update relaxes any of them                                                                                                                                                                                            |
| 7   | `mental-model`                                            | One-sentence Composer/Ratchet/Vault metaphor                                                                                                                                                                                                                                                     | **Kept as-is**                   | Still the right high-level mental model; new components (deploy-gate strategies, the babysit loop, ops tooling) are refinements _within_ the "Ratchet" piece of this metaphor, not replacements for it                                                                                          |
| 8   | _(new)_ "What changed since v1"                           | —                                                                                                                                                                                                                                                                                                | **Added**                        | New section required by this mission: covers the three deploy-gate strategies, the self-healing/babysit loop, the sandboxed multi-pass tester, the append-only bug ledger, and per-invocation timeouts — the concrete things that changed since the loop described in row 5 was first published |
| 9   | _(new)_ "Learnings"                                       | —                                                                                                                                                                                                                                                                                                | **Added**                        | New section required by this mission: transferable principles for anyone running an autonomous build-deploy-test loop, generalized from the same changes in row 8 — written as principles, not anecdotes, so nothing identifies specific infrastructure                                         |
| 10  | `topics` ("Topics")                                       | Data-driven grid of 8 module cards (Ratchet system guide, Rebuild checklist, Writing missions, Live verify & testing, Models & costs, Case studies, Composer walkthrough, Vault deep-dive) rendered from `vibeCodingModules`, plus a `<script type="application/json">` payload of the same list | **Kept as-is**                   | Module inventory and routing are unaffected by this rewrite; the module list is shared with other pages and out of scope                                                                                                                                                                        |
| 11  | `cta` (Final CTA)                                         | Heading, body, 3 CTAs (Start free → `/apply`, Read the guide, Rebuild checklist)                                                                                                                                                                                                                 | **Kept as-is**                   | Same reasoning as row 1; `/apply` destination is asserted by the CTA e2e spec                                                                                                                                                                                                                   |

## Code snippets / diagram references in v1

| Item                                               | v1 location             | Disposition                                                                                                                              | Reason                                                                                                                         |
| -------------------------------------------------- | ----------------------- | ---------------------------------------------------------------------------------------------------------------------------------------- | ------------------------------------------------------------------------------------------------------------------------------ |
| ASCII/step-list loop diagram (`VibeLoopDiagram`)   | `loop` section          | Kept                                                                                                                                     | Renders from the same `content.loop.steps` data, unchanged                                                                     |
| `vibe-coding-modules` JSON `<script>` payload      | End of `topics` section | Kept                                                                                                                                     | Machine-readable mirror of the topics grid; no content in it changed                                                           |
| No inline code/CLI snippets existed in v1 hub copy | —                       | v2 **adds** one generic worked example (env-var-style deploy-gate config with placeholder domain/repo) in the new "What changed" section | Mission requires at least one placeholder-style example (`example.com`, `your-org/your-repo`, or `$VAR`) to appear on the page |

## Workflow steps described in v1

The only "workflow" v1 describes is the 6-step ratchet loop (row 5) and the
3-step setup flow (row 2). Both are kept unchanged; v2 adds narrative around
what happens _inside_ the Build → deploy-gate → Test steps without changing
the steps themselves, so the existing `VibeLoopDiagram` and `StepList` outputs
remain accurate.

## Net result

- **0 sections dropped.** Nothing in v1 was factually wrong or worth removing;
  v2 is additive (rows 8–9 are new), not a subtractive rewrite.
- **2 sections added** (rows 8–9), satisfying the mission's requirement for a
  "what changed since v1" narrative and a distinct "Learnings" section.
- All CTA hrefs, `data-section` anchors used by
  `apps/web/e2e/vibe-coding-cta.spec.ts`, and the `vibe-coding-modules` JSON
  contract are preserved untouched.
